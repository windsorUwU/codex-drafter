# Assets

`codex-banner.png` is intended to be the canonical generated dark Codex pony banner used in PR footers.

The MCP server does not require the binary to run. Until the image is committed here, set `CODEX_BANNER_URL` to a public copy of the banner you want rendered in pull requests.
